package controller;
import java.sql.*;

public class ControleurConnexion {
	public ControleurConnexion(int choix) {
			
		// Si le user a cliqué sur le bouton "connexion" :
		if(choix == 1) {
			System.out.println("totototototottatatatatatttttitititiititit");
		// --> Comparer les chaînes de caractères avec celles de la BDD (isset et !empty)
		// --> Si OK : affichage de la vue Accueil
		
		
		}
	
		// Si le user a cliqué sur le bouton "creation" :
		if(choix == 2) {
		// --> Affichage de la vue Création de Compte
		
		}
}
}